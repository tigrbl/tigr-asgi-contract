{
  "$id": "https://tigr.dev/asgi-contract/schemas/events/stream.resume.reject.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "not": {
    "required": [
      "subsurface"
    ]
  },
  "properties": {
    "binding": {
      "enum": [
        "http.stream",
        "http.sse",
        "ws",
        "wss",
        "webtransport"
      ]
    },
    "client_id": {
      "minLength": 1,
      "type": "string"
    },
    "reason": {
      "enum": [
        "expired",
        "identity_mismatch",
        "not_found",
        "policy_denied",
        "out_of_window"
      ]
    },
    "resume_token": {
      "minLength": 1,
      "type": "string"
    },
    "session_id": {
      "minLength": 1,
      "type": "string"
    },
    "stream_id": {
      "oneOf": [
        {
          "type": "integer"
        },
        {
          "minLength": 1,
          "type": "string"
        }
      ]
    },
    "type": {
      "const": "stream.resume.reject"
    }
  },
  "required": [
    "type",
    "resume_token",
    "client_id",
    "session_id",
    "stream_id",
    "binding",
    "reason"
  ],
  "title": "stream.resume.reject event payload",
  "type": "object"
}
